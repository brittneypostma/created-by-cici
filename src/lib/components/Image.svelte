<script>
	/**
	 * @type {string}
	 */
	export let src
	/**
	 * @type {string}
	 */
	export let alt

	// const name = /.*\//
	// img.replace(name, '').split('.').slice(0, -1).join('.')
</script>

<img {src} {alt} class="rounded-lg bg-cover mb-4" />
